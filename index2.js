// 2.Write an arrow function that takes in an array of numbers and returns an array of the same numbers 
const returnNumbers = (arr) => {
    return arr;
  }

          
const returnNumbersAscending = arr => {
    return arr.sort((a, b) => a - b);
  }

     